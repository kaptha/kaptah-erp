import { AccountPayableEntity } from './account-payable.entity';

describe('AccountPayableEntity', () => {
  it('should be defined', () => {
    expect(new AccountPayableEntity()).toBeDefined();
  });
});
