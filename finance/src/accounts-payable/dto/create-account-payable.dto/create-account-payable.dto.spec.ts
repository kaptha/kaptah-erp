import { CreateAccountPayableDto } from './create-account-payable.dto';

describe('CreateAccountPayableDto', () => {
  it('should be defined', () => {
    expect(new CreateAccountPayableDto()).toBeDefined();
  });
});
