import { AccountReceivableEntity } from './account-receivable.entity';

describe('AccountReceivableEntity', () => {
  it('should be defined', () => {
    expect(new AccountReceivableEntity()).toBeDefined();
  });
});
